{
  "name": "deepavatar.xyz",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "rm -rf node_modules/.cache && next build && next export && touch out/.nojekyll && rm -rf docs && mv out docs && git add docs && git commit -m \"docs(build): deploy\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@zeit/next-css": "^1.0.1",
    "next": "^7.0.0",
    "react": "^16.5.2",
    "react-dom": "^16.5.2",
    "react-dropzone": "^5.1.1",
    "styled-jsx": "^3.1.0",
    "validate.js": "^0.12.0"
  },
  "devDependencies": {
    "babel-plugin-transform-define": "^1.3.0"
  }
}
